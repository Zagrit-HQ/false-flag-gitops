apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: falseflag-preview-__ENVIRONMENT__
  namespace: argocd
  annotations:
    falseflag.dev/created-at: "__CREATED_AT__"
  labels:
    app.kubernetes.io/name: falseflag
    falseflag.dev/environment: preview
    falseflag.dev/preview: __ENVIRONMENT__
    falseflag.dev/pr: "__PR_NUMBER__"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    - repoURL: https://github.com/Zagrit-HQ/false-flag-demo.git
      targetRevision: refs/heads/__BRANCH__
      path: deploy/helm/falseflag-operator
      helm:
        releaseName: falseflag
        valueFiles:
          - $values/preview/__ENVIRONMENT__/values.yaml
        parameters:
          - name: image.tag
            value: $ARGOCD_APP_REVISION_SHORT_8
            forceString: true
    - repoURL: https://github.com/Zagrit-HQ/false-flag-gitops.git
      targetRevision: refs/heads/main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: __NAMESPACE__
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 20
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 2m
